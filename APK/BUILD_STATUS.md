# APK build status

Expected output filename: `APK/Warpath_v0_0_1.apk`

**Status: BUILD SUCCESSFUL**

The APK has been built successfully. Output: `APK/Warpath_v0_0_1.apk` (5.9 MB debug build).

To rebuild locally on a machine with Android SDK + network access:

```bash
./scripts/build_apk.sh
```
